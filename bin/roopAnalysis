#!/bin/bash
cat ../listMain.txt | while read line || [[ -n "${line}" ]];
do
	if [[ ${line} != //* ]];
	then 
		nuclide=`echo ${line} | cut -d ',' -f1`
		echo "name: ${nuclide}"
		./bin/Analysis /home/data/SHARAQ/sh13/tmpsort21/ge_sh_all_dn21_20000000.root ${nuclide}
	fi
done
